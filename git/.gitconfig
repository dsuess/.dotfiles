[user]
    email = suess.daniel@gmail.com
    name = Daniel Suess
    signingkey = /Users/dsuess/.ssh/id_rsa
[core]
    editor = nvim
    excludesfile = ~/.gitignore
[push]
    default = simple
	autoSetupRemote = true
[merge]
    tool = nvimdiff
    conflictstyle = diff3
[diff]
    tool = nvimdiff
[difftool]
    prompt = false

[difftool "nvimdiff"]
    cmd = nvim -d $LOCAL $REMOTE
[mergetool "nvimdiff"]
    cmd = nvim -d $BASE $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'

[alias]
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    lv = log --reverse --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    broom = "! git rm $(git ls-files --deleted)"
[github]
    user = dsuess
[commit]
	gpgsign = true
[tag]
	gpgsign = true
[gpg]
	format = ssh
