#!/bin/bash


export GHT=$(op read "op://Private/5alxdu2lqx2bsqercudvvoekce/password")

CMD="
touch .no_auto_tmux && \
git config --global user.name 'Daniel Suess' && \
git config --global user.email 'suess.daniel@gmail.com' && \
git clone https://dsuess:$GHT@github.com/dsuess/triton-kernels -b dev
"

ssh vast.ai $CMD
scp ~/.tmux.conf vast.ai:
