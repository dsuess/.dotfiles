{
	"name": "pi-extension-plan-mode",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"clean": "echo 'nothing to clean'",
		"build": "echo 'nothing to build'",
		"check": "npm test",
		"test": "node --test tool-result.test.mjs"
	},
	"pi": {
		"extensions": [
			"./index.ts"
		]
	}
}
